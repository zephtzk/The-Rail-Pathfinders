# Assessment evidence

These reference copies preserve the recorded verification results and browser-test source byte for byte from source snapshot `d787c8d6501b3027d21cefd734e46cd726f5c65c`. Use the pinned source ZIP linked in the assessment write-up to run the original test suite. The `.mjs.txt` file is a source reference, not a standalone executable.
