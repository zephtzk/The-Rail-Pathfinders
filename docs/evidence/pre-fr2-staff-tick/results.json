{
  "checks": [
    "staff without an accepted trip retains its no-dispatch explanation and has no reset tick",
    "fresh default is simple guidance without saving an explicit preference",
    "automatic current instruction has no reset button",
    "platform message applies immediately and exposes the reset tick",
    "transfer message applies immediately and exposes the reset tick",
    "exit message applies immediately and exposes the reset tick",
    "lift message applies immediately and exposes the reset tick",
    "alert message applies immediately and exposes the reset tick",
    "compact tick has a meaningful accessible name and tooltip with no visible text",
    "click resets to the actual current instruction, hides the tick and focuses the dropdown",
    "Enter reset tick directly follows the dropdown in keyboard order",
    "Enter keyboard focus is visibly outlined",
    "Enter resets the message without changing journey state",
    "Space reset tick directly follows the dropdown in keyboard order",
    "Space keyboard focus is visibly outlined",
    "Space resets the message without changing journey state",
    "staff message choices and tick activation dispatch no journey state changes",
    "offline refresh preserves manual message and request focus",
    "online refresh preserves manual message and request focus",
    "hidden refresh preserves manual message and request focus",
    "visible refresh preserves manual message and request focus",
    "offline refresh preserves manual message and automatic focus",
    "online refresh preserves manual message and automatic focus",
    "hidden refresh preserves manual message and automatic focus",
    "visible refresh preserves manual message and automatic focus",
    "leaving and returning to staff preserves a manual selection",
    "selecting automatic in the dropdown itself restores the current instruction",
    "an unconfirmed journey picker selection preserves the manual staff message",
    "a confirmed current-step change automatically refreshes the card and removes the reset tick",
    "accepted route replacement resets manual staff text while staff remains open",
    "full staff tick matches current-step tick colour, border, size and glyph",
    "full mobile keeps dropdown and tick adjacent without horizontal overflow",
    "full mobile retains 48px touch targets",
    "full mobile dropdown text respects the root text size",
    "full mobile reset remains reachable and leaves journey unchanged",
    "full 320-large-text keeps dropdown and tick adjacent without horizontal overflow",
    "full 320-large-text retains 48px touch targets",
    "full 320-large-text dropdown text respects the root text size",
    "full 320-large-text reset remains reachable and leaves journey unchanged",
    "simple staff tick matches current-step tick colour, border, size and glyph",
    "simple mobile keeps dropdown and tick adjacent without horizontal overflow",
    "simple mobile retains 56px touch targets",
    "simple mobile dropdown text respects the root text size",
    "simple mobile reset remains reachable and leaves journey unchanged",
    "simple 320-large-text keeps dropdown and tick adjacent without horizontal overflow",
    "simple 320-large-text retains 56px touch targets",
    "simple 320-large-text dropdown text respects the root text size",
    "simple 320-large-text reset remains reachable and leaves journey unchanged",
    "staff controls make no sharing requests or network mutations",
    "staff tick flows report no browser exceptions"
  ],
  "passed": 50,
  "errors": [],
  "sharingRequests": [],
  "mutations": [],
  "fixture": "Synthetic accepted journey; actual UI step confirmation and synthetic accepted-route replacement event",
  "environment": "Desktop Edge mobile/touch emulation, keyboard, 320px and 200% text; map tiles blocked; no physical-phone, provider-status or FR2 tester-session claim"
}